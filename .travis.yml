language: node_js
node_js:
  - lts/*
install: 
  - npm install
  - cp ./secrets/sample-config.json ./secrets/config.json
jobs:
  include:
    - script: tsc -p ./tsconfig.json
    - script: COVERALLS_FLAG_NAME=coverage npm run coverage
notifications:
  webhooks: https://coveralls.io/webhook